# docker-compose.yml

services:
  web:
    build:
      context: .
    env_file:
      - .env
    expose:
      - "8000"  # Internal port, not exposed directly
    restart: unless-stopped

  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - web
    restart: unless-stopped
