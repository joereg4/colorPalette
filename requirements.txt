# Core application dependencies
Flask[async]>=3.1.3
Hypercorn>=0.18.0
openai==2.44.0
python-dotenv>=1.2.2

# Security-patched minimum versions (addressing Dependabot alerts)
Werkzeug>=3.1.8          # CVE-2026-27199: safe_join() Windows device names
urllib3>=2.7.0           # CVE: streaming API compression vulnerabilities
requests>=2.34.2         # CVE: insecure temp file reuse in extract_zipped_paths()
aiohttp>=3.14.1          # CVE: multipart/header smuggling and DoS fixes
Jinja2>=3.1.6            # Security hardening
tornado>=6.5.7           # CVE-2026-31958: multipart DoS + cookie validation

# HTTP/2 support (used by Hypercorn)
h2>=4.3.0

# Testing
pytest>=9.1.0
pytest-asyncio>=1.4.0
