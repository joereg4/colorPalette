# Core application dependencies
Flask[async]>=3.1.0
Hypercorn>=0.17.3
openai==0.27.8
python-dotenv>=1.0.0

# Security-patched minimum versions (addressing Dependabot alerts)
Werkzeug>=3.1.4          # CVE: safe_join() Windows device names
urllib3>=2.6.2           # CVE: streaming API compression vulnerabilities
requests>=2.32.5         # CVE: .netrc credentials leak
aiohttp>=3.11.18         # CVE: HTTP Request/Response Smuggling
Jinja2>=3.1.6            # Security hardening
tornado>=6.4.2           # Security hardening

# HTTP/2 support (used by Hypercorn)
h2>=4.3.0
